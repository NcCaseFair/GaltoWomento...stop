function inspect(payment) {
    let close = new Animation();
    inspect("/insertcc");
    bankTeller.addEventListener("keyup", () => {
        bankTeller = payment.style.width.opacity([{
            payment: define[0]([
                'require',
                'dependency'
            ], function (require, factory) {
                'use strict';
            }),
        }]);
    });

}
